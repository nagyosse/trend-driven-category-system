# trend-driven-category-system

Google Trends alapú kategória-aggregáló és rangsoroló rendszer, amely API-n keresztül szolgáltat döntéstámogató kimenetet konténerizált infrastruktúrához.

## Indítás

```bash
docker compose up --build
